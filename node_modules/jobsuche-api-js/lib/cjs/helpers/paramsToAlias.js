"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paramsToAlias = paramsToAlias;
const alias_1 = require("../constants/alias");
function paramsToAlias(params) {
    const translatedParams = {};
    for (const key in params) {
        if (params.hasOwnProperty(key)) {
            const alias = alias_1.aliasMapping[key];
            if (alias) {
                if (key === 'workTime' && params[key]) {
                    // For workTime, join the array into a semicolon-separated string
                    translatedParams[alias] = params[key].join(';');
                }
                else {
                    translatedParams[alias] = params[key];
                }
            }
        }
    }
    return translatedParams;
}
